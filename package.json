{
  "name": "powerstrip-weave",
  "version": "0.0.0",
  "description": "HTTP endpoint for the powerstrip-weave plugin",
  "main": "index.js",
  "scripts": {
    "test": "set -e; for t in test/*.js; do node $t; done"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/binocarlos/powerstrip-weave.git"
  },
  "author": "Kai Davenport",
  "license": "MIT",
  "dependencies": {
    "concat-stream": "^1.4.7",
    "minimist": "^1.1.0"
  },
  "devDependencies": {
    "tape": "^3.4.0",
    "async": "^0.9.0"
  }
}
